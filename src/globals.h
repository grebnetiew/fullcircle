#pragma once

enum PersistKeys {
  KEY_COL_HOUR = 1,
  KEY_COL_MINUTE = 2,
  KEY_COL_APPOINTMENT = 3,
  KEY_COL_CIRCLE = 4,
  KEY_COL_BACKGROUND = 5,
  KEY_COL_DATE = 6,
  KEY_CALENDAR = 7,
};

enum DataKeys {
  CAL_VER_KEY = 0,
  CAL_DATA_KEY = 1,
  CAL_DATA_LENGTH = 40,
};
